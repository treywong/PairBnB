<h1>Edit your profile</h1>
<%= form_for @user, url: users_update_path(@user) do |form| %>
	<%= render partial: '/users/form', object: form %>

	<div class="submit-field">
		<%= form.submit %>
	</div>
<% end %>