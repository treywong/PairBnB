<h1>Your Reservations</h1>

<% @reservation.each do |reserve| %>
	<a href="/listing/<%= reserve.listing.id %>">
		<h2><%= reserve.listing.name %></h2>
	</a>
	<p><%= reserve.listing.detail %></p>
	<p>From <%= reserve.date_start %> to <%= reserve.date_end %></p>
<% end %>
